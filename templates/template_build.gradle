// CHANGE FOR NEW WEBSITE: Replace 'mysite' with the site name
apply plugin: "kei.plugins.extension.legacy"
apply plugin: 'kotlinx-serialization'

ext {
    extName = 'MySite' // CHANGE FOR NEW WEBSITE
    pkgNameSuffix = 'en.mysite' // CHANGE FOR NEW WEBSITE
    extClass = '.MySite' // CHANGE FOR NEW WEBSITE (must match class name in .kt)
    extVersionCode = 1
    isNsfw = false
}

dependencies {
    // DO NOT MODIFY: Core framework utilities
    implementation(project(':core'))

    // OPTIONAL: Uncomment extractors based on what the target website uses
    // Use colons (:) for library project paths, not hyphens (-)
    // implementation(project(':lib:playlistutils'))
    // implementation(project(':lib:filemoonextractor'))
    // implementation(project(':lib:mp4uploadextractor'))
    // implementation(project(':lib:streamwishextractor'))
    // implementation(project(':lib:unpacker'))
    // implementation(project(':lib:cloudflareinterceptor'))
}

// DO NOT MODIFY: Standard build script (handled by kei.plugins.extension.legacy)
